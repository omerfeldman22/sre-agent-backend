apiVersion: v1
kind: Service
metadata:
  name: sre-agent-backend
  labels:
    app: sre-agent-backend
spec:
  type: LoadBalancer
  selector:
    app: sre-agent-backend
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
